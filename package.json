{
  "private": true,
  "name": "discord-interactions-worker",
  "version": "1.0.0",
  "description": "Simple kickstart for Discord interactions using Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "build": "webpack",
    "build:watch": "webpack --watch"
  },
  "author": "Remco Jongschaap <hey@dougley.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2",
    "webpack": "^5.11.0",
    "webpack-cli": "^4.2.0"
  },
  "dependencies": {
    "tweetnacl": "^1.0.3"
  }
}
